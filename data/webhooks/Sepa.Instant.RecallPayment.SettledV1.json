{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "SepaInstantRecallPaymentSettled",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "TransactionId",
    "OriginalTransactionId",
    "TotalReturnedAmount",
    "TimestampSettled"
  ],
  "properties": {
    "TransactionId": {
      "type": "string",
      "description": "Unique identifier of the recall payment as assigned by ClearBank.",
      "maxLength": 35,
      "minLength": 1
    },
    "OriginalTransactionId": {
      "type": "string",
      "description": "Unique identifier of the original payment transaction, as assigned by ClearBank, to unambiguously identify the transaction. This identifier is passed on, unchanged, throughout the entire end-to-end chain.",
      "maxLength": 35,
      "minLength": 1
    },
    "TotalReturnedAmount": {
      "type": "number",
      "description": "The amount of money returned."
    },
    "TimestampSettled": {
      "type": "string",
      "description": "Date and time the recall payment was settled, in UTC.",
      "format": "date-time"
    }
  }
}