---
title: "Manage real accounts"
metaTitle: "Manage real accounts"
order: 2
showPageMenu: true
---

import EndpointBlock from "src/components/endpoint-block";
import WebhookPlaceholder from 'src/components/webhook-placeholder'
import WebhookSummary from 'src/components/webhook-summary'
import Callout from 'src/components/callout'

## Manage real accounts

A real account held with ClearBank and can be used to send and/or receive payments. The balance of a real account is also held by ClearBank.

**Before creating an account, please ensure that you know which [account type](./account-types) you need to create.** If you are unsure, please get in touch.

<Callout colour="blue">
At this time, you can only hold euro (EUR) balances in a European real account. When you create the account, set <strong>currencies</strong> to <strong>EUR</strong>.
</Callout>

GBP payments to and from a European account are not allowed and would be rejected and returned automatically. For more information about currencies supported by ClearBank accounts, please speak to your Client Director.

- To create and manage virtual accounts, refer to [Manage accounts](./manage-virtual-accounts).

<EndpointBlock
  type="post"
  title="Create a new account (real)"
  endpoints={[
        {
      path: "/mccy/v2/Accounts",
      version: "2.0.MCCY-ACC-EU",
      webhooks: [
        'mccy-account-created-webhook-v1'
      ]
    }
  ]}
/>

<WebhookPlaceholder render='mccy-account-created-webhook-v1' />

<EndpointBlock
  type="get"
  title="Get all accounts (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts",
      version: "1.0.MCCY-ACC-EU"
    }
  ]}
/>

<EndpointBlock
  type="get"
  title="Get information for an account (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts/{accountId}",
      version: "1.0.MCCY-ACC-EU"
    }
  ]}
/>

<EndpointBlock
  type="patch"
  title="Amend the properties of an account (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts/{accountId}",
      version: "1.0.MCCY-ACC-EU",
      webhooks: [
        'mccy-account-updated-webhook-v1'
      ]
    }
  ]}
/>

<WebhookPlaceholder render='mccy-account-updated-webhook-v1' />

<EndpointBlock
  type="patch"
  title="Update the status of an account (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts/{accountId}/status",
      version: "1.0.MCCY-ACC-EU",
      webhooks: [
        'mccy-account-updated-webhook-v1'
      ]
    }
  ]}
/>

<EndpointBlock
  type="get"
  title="Get the balance of an account (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts/{accountId}/balances",
      version: "1.0.MCCY-ACC-EU"
    }
  ]}
/>

<EndpointBlock
  type="delete"
  title="Close an existing account (real)"
  endpoints={[
    {
      path: "/mccy/v1/Accounts/{accountId}",
      version: "1.0.MCCY-ACC-EU",
      webhooks: [
        'mccy-account-updated-webhook-v1'
      ]
    }
  ]}
/>