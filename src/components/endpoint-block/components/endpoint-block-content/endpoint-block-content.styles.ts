import styled from 'styled-components'

import { colors } from 'src/components/theme'

export const Enums = styled.div`
  font-size: 12px;
  color: ${colors.brandSecondaryDarker};
  font-family: 'Roboto Mono', monospace;
`
